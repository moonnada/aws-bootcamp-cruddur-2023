#! /usr/bin/bash

if [ -z "$1" ];

then echo "No TASK_ID argument supplied eg ./bin/ddb/drop cruddur-messages prod " exit 1 fi TASK_ID = $1

aws ecs execute-command  \
--region $AWS_DEFAULT_REGION \
--cluster cruddur \
--task dceb2ebdc11c49caadd64e6521c6b0c7 \
--container backend-flask \
--command "/bin/bash" \
--interactive
